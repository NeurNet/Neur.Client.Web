# Neur.Client.Web

## How to start

1. [Install Docker](https://www.docker.com/get-started/)
2. Run this commands in project folder:

```
$ docker build -t neur_client_web .
$ docker run --name neur_client_web -p 80:80 -d neur_client_web
```
